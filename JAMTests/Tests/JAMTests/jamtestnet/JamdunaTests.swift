@testable import JAMTests
import Testing
import Utils

struct JamdunaTests {
    // @Test(arguments: try JamTestnet.loadTests(path: "traces", src: .jamduna))
    // func traceTests(_ input: Testcase) async throws {
    //     try await TraceTest.test(input)
    // }
}
